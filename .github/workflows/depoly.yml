name: Depoly

on:
  push:
    branches:
      - main
      

jobs:
  build:
    name: Build
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 2
    - uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - run: make local
